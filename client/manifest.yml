applications:
  - name: anant-cars-frontend
    buildpacks:
      - staticfile_buildpack
    path: dist
    memory: 64M

    # cf push anant-cars-frontend -b staticfile_buildpack
    # cf push anant-cars-backend
    
    # Staticfile
    # pushstate: true
